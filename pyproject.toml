[project]
name = "pyfmindex"
version = "0.0.1"
description = "Python wrapper for AvxWindowFmIndex library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "debugpy>=1.8.17",
    "pytest>=8.4.2",
]
